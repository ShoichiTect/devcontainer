# Environment variables template for Claude Code isolated environment
#
# This file is automatically copied to .env in the container workspace.
# The host's .env file (if exists) is NOT mounted to protect secrets.
#
# Fill in your environment variables below as needed:

# Example variables (uncomment and set values as needed)
# API_KEY=
# DATABASE_URL=
# SECRET_TOKEN=
# NODE_ENV=development
